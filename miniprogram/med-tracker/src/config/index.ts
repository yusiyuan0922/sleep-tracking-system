// 配置文件
export const config = {
  // API基础URL
  baseURL: 'http://localhost:3000',

  // 超时时间
  timeout: 10000,

  // Token存储key
  tokenKey: 'access_token',

  // 用户信息存储key
  userInfoKey: 'user_info',
};

export default config;
